# 🚀 Space Joops 밸런스 및 튜닝 가이드

이 문서는 게임의 속도, 연료 소모, 판정 등 핵심 밸런스를 조절하는 방법을 설명합니다.
게임 로직이나 물리 공식을 직접 수정할 필요 없이, 아래 명시된 **TUNE 변수**들만 수정하여 게임의 전체 느낌(게임성)을 변경할 수 있습니다.

---

## 🛠️ 수정해야 할 파일 위치
모든 밸런스 상수는 다음 파일의 상단 `TUNE` 객체에 모여 있습니다.
**파일 경로:** `src/app/play/joops-game.tsx` (약 42번째 줄 시작 부분)

---

## 🏎️ 속도 및 이동 관련 튜닝

우주선의 이동 속도와 관성은 아래 변수들을 수정하여 조절합니다.

### 1. `thrustSpeeds` (추진 강도별 속도)
* **역할:** 조이스틱을 당겼을 때 우주선이 나아가는 가속도/최고 속도를 결정합니다.
* **구조:** `[약한 추진, 중간 추진, 강한 추진]` 3단계 배열 형태입니다.
* **수정 방법:** 배열 안의 숫자를 높이면 더 빠르게 날아갑니다.
* **현재 코드 예시:**
  ```typescript
  thrustSpeeds: [150, 375, 675], // 50% 향상된 수치
  ```

### 2. `minSpeed` (최소 유지 속도)
* **역할:** 조이스틱에서 손을 놓아도 우주 공간의 무중력 상태처럼 항상 최소한으로 유지되는 "기본 표류 속도"입니다.
* **수정 방법:** 값을 높이면 화면이 기본적으로 더 빠르게 흘러가 템포가 빨라집니다. 0으로 설정하면 조이스틱에서 손을 뗄 경우 우주선이 완전히 멈춥니다.
* **현재 코드 예시:**
  ```typescript
  minSpeed: 67.5,
  ```

### 3. `friction` (마찰력 / 우주 관성)
* **역할:** 조이스틱에서 손을 떼었을 때 속도가 얼마나 빨리 줄어드는지 결정합니다.
* **수정 방법:** 숫자가 클수록 우주선에 급브레이크가 걸리듯 빨리 멈추고, 숫자가 작을수록 미끄러지듯 오랫동안 날아갑니다.
* **현재 코드 예시:**
  ```typescript
  friction: 1.2,
  ```

---

## ⛽ 연료(Fuel) 밸런스 튜닝

연료 시스템의 난이도는 아래 세 가지 값의 조합으로 결정됩니다.

### 1. `maxFuel` (최대 연료량)
* **역할:** 화면 하단에 표시되는 연료 게이지의 최대치입니다. 상점 업그레이드 전 기본 수치입니다.
* **현재 코드 예시:** `maxFuel: 3000,`

### 2. `fuelRegen` (연료 자연 회복량)
* **역할:** 추진기를 사용하지 않고 있을 때(또는 약하게 쓸 때) 1초당 회복되는 연료의 양입니다.
* **수정 방법:** 값을 높이면 배터리가 아주 빠르게 차오르고 난이도가 크게 낮아집니다.
* **현재 코드 예시:** `fuelRegen: 600,`

### 3. `thrustCosts` (추진 강도별 소모량)
* **역할:** `thrustSpeeds`의 3단계에 대응하여, 1초당 소모되는 연료의 양입니다.
* **수정 방법:** 강한 추진(세 번째 값)의 비용을 매우 높이면 플레이어가 조이스틱을 항상 끝까지 당겨 쓰는 대신 살살 컨트롤하도록 유도할 수 있습니다.
* **현재 코드 예시:**
  ```typescript
  thrustCosts: [20, 80, 200],
  ```

---

## 💡 요약: 원하는 느낌에 따른 셋팅 팁

- **조작감이 무겁고 살얼음판 걷는 느낌을 주고 싶다면?**
  👉 `friction`을 `0.5` 정도로 낮추고, `minSpeed`를 `100` 이상으로 높이세요.
- **아주 스피디하고 아케이드적인 느낌을 원한다면?**
  👉 `thrustSpeeds`를 `[200, 500, 900]`처럼 크게 늘리고, 그만큼 `friction`을 `2.0`으로 높여 제동력이 즉각적으로 작용하게 만드세요.
- **조작이 너무 어려워서 유저를 배려하고 싶다면?**
  👉 `fuelRegen`을 늘려주거나 `thrustCosts`를 50% 정도 낮춰주면 됩니다.
